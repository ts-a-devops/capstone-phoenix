apiVersion: v1
kind: Secret
metadata:
  name: taskapp-secrets
  namespace: taskapp
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME"
  DATABASE_PASSWORD: "REPLACE_ME"
  # Replace these placeholder values in CI or with a sealed secret before production.
  # Do not keep plaintext secrets in git for production deployments.
